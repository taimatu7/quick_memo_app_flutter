import 'package:flutter/material.dart';

class CommonColors {
  static final ColorScheme themeColorScheme =
      ColorScheme.fromSeed(seedColor: Colors.orange);
  static const Color themeColor = Colors.orange;
}
